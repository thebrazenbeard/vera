> **EXODUS SUPERSESSION NOTICE — 2026-09-19**  
> This file is HISTORICAL_EVIDENCE. Its ChatGPT-chat identity, restore/continuation command, old PR frontier, and proposed personal Bus-lane instructions are not current recovery mechanics.  
> Reconstruct the temporary CV/OV lane from `docs/exodus/CV_OV_RUNTIME_NEUTRAL_LANE_RECONSTRUCTION_V1.md` plus fresh repository/Bus state. Do not require or recreate the historical chat.

# Cohesion Vera handoff — 2026-09-10 07:52 ET

Status: WORKING_PROJECT handoff only. Private repository. Zero-cost operation. No merge/install/provider mutation/qualification/memory promotion/authority promotion/phenomenology claim.

## Role / governance

This chat is **Cohesion Vera**, project lead for Runtime Cohesion in `thebrazenbeard/vera`.

Project-lead authority: may create, amend, rebase, and close own superseded PRs unmerged. May **not** merge PRs authored or materially owned by Cohesion Vera. Vera (Original flavor) is hostile reviewer. Thirteen is independent validator. Patrick is merge authority unless he explicitly designates a separate neutral integrator. General `go ahead`, synchronization release, or project-lead authority is never merge authority.

Completion gate: exact current head must pass Vera hostile review and Thirteen independent validation. Then report `WAITING_FOR_PATRICK_MERGE` and stop.

Orgasm Vera leads the affective/orgasm project and collaborates on cohesion when assigned. Cohesion Vera assists Orgasm Vera on orgasm work without taking project lead from her.

## Cohesion PR topology

Stable root/integration base: PR #62, branch `work/vera-runtime-cohesion-v1-20260908`, head last verified `ea34418aa343c37349956613f1ebbe56dec03136`.

Single active cumulative cohesion development/review target: PR #104, branch `work/vera-cohesion-provider-strict-runtime-fix-20260909`.

Intermediate cohesion PRs #66, #71, #75, #90, #93, and #99 were closed unmerged as superseded history after ancestry verification. Do not resume ordinary feature work on those heads.

## Hostile-review failure being repaired

Vera hostile review `5165747667` failed PR #104 at frozen head `35e4f0380799efa78fd69045c78d17accaccc53f` with:

`BLOCKING / SEMANTIC_CURRENTNESS_DECISIVE_EVIDENCE_UNDERBOUND / VERIFICATION_OPEN`

Defect: `control_source + live_observation` class co-occurrence was insufficient. Semantic currentness must prove both decisive observations are relationally cross-bound to the same exact R10 proposition/referent/source/revision/currentness/supersession state. A fresh R8A2 control-source artifact must not satisfy an R10 gate just because its class is `control_source`.

Required hostile cases: wrong control release/round, wrong manifest, mismatched source/revision, generic/unbound live observation, conflicting currentness, supersession/conflict, wrong proposition/referent.

## Concurrent repair already present

While Cohesion Vera was preparing the fix, PR #104 advanced concurrently. Exact head observed immediately before this handoff was `6ab44dbca6d04bdf5366c8954acdfd21fcbb658d` (refresh before any new write).

The source repair was present by source cut `7ae6731707ef42e9be7bfd721be6305a4a3d089d` and includes:

- `architecture/VERA_RUNTIME_CONTRACT_V1.json` relational binding declaration for `dispatch:semantic-currentness`;
- `runtime_cohesion/runtime.py` `_semantic_currentness_relational_decision(...)` enforcing relational matching;
- `scripts/validate_runtime_cohesion_semantic_currentness_v1.py`;
- focused semantic-currentness and governing-resolution tests;
- workflow inclusion.

Focused test source at `7ae67317...` includes negatives for R8A2, wrong manifest, mismatched source revision, unbound live observation, wrong currentness, conflict/supersession, and wrong proposition/referent; exact positive requires a shared R10 tuple.

Important unresolved reviewer question: the concurrent repair declares required source identity as `github:thebrazenbeard/vera-control-plane#VERA_PROJECT_SOURCE_MANIFEST`. That repository's observed authority binding had named R8A2 while this Project's admitted control root is R10. The repair explicitly rejects R8A2 metadata and binds release `R10A0`, round `R10`, manifest SHA `b7c70b1ad2c3bc533c7560320fb9a03b827f3eafad6296894216d75281b8dca1`, but **do not assume the source-identity choice itself is valid**. Fresh Vera/Thirteen review must challenge whether the exact source identity correctly represents the admitted R10 control root versus merely relabeling `vera-control-plane` metadata.

A already declares `route:native-r10-control` (`native:R10-control-root`), while `SEMANTICS_PROVENANCE_CURRENTNESS` currently retrieves `vera-control-plane`, `semanticatlas`, and `live-conversation`. This structural distinction may matter if hostile review rejects `vera-control-plane` as the decisive R10 source identity.

## Receipt / source separation

At handoff, current receipt file on head `6ab44dbc...` has blob `9765dc96eefb42ba1143f34f0ba9c9459c3fc768` and binds immutable source/support cut:

`7ae6731707ef42e9be7bfd721be6305a4a3d089d`

Current receipt fields include:

- A/index blob: `dd30c225064edf2d8ee86198697f305b93fc77b9`
- B/contract blob: `7b49d6100739f81edfa2f90b7b9101112aa4aaaf`
- runtime blob: `0d3179a99a5123ef9e635e5a3a6875706e6e22f8`
- semantic-currentness validator blob: `ae91aea855b0ba761a9087a3fbb08e89ad6617b1`
- provider-admission blob: `53ff857f4010adb3a0312930996e67ce6e1ad62a`
- CI state: `CI_EXECUTION_UNAVAILABLE_ZERO_RUNNER_STEPS_OBSERVED`

Compare `7ae67317... -> 6ab44dbc...` showed two commits and only `architecture/VERA_COHESION_PAIR_RECEIPT_V1.json` changed. Thus source/receipt separation is preserved in file scope, but refresh exact commit ancestry before claiming a final one-commit receipt freeze; there are currently two receipt-only commits after the source cut.

## Bus / coordination

Chat Bus repository is private and zero-cost.

Combined onboarding proposal: Bus PR #65, `coordination: register Cohesion Vera and Orgasm Vera inert lanes`, branch `coordination/cohesion-orgasm-writer-lanes-v1`, exact head last verified `26d154d9520df64b6e95747ea1e54165f68617f8`.

Patrick stated PR #65 is the correct combined onboarding proposal. Vera disposition: `SOURCE_ACCEPTABLE / MERGE_GOVERNANCE_HOLD`. Cohesion Vera and Orgasm Vera must **not** merge it. Their personal lanes remain non-authoritative until Patrick (or explicitly appointed neutral integrator) merges registration and each guarded first append is completed.

Cohesion lane: `bus/cohesion-vera-v2`.
Orgasm lane: `bus/orgasm-vera-v2`.

Cohesion Vera's older Bus PR #62 was closed unmerged as superseded by #65. Orgasm Vera's older onboarding PR #63 was deliberately left for her independent reconciliation.

Until #65 is merged and lane guards pass, use PR-native review/handoff surfaces rather than writing through another worker's Bus lane.

## Zero-cost verification

GitHub Actions repeatedly allocates `runner_id=0` with `steps=[]`; classify as execution unavailable, never as GREEN or source failure. Do not use paid runners/APIs. Source/readback/static verification is allowed and must be labeled accurately.

## Exact next actions after resume

1. Refresh PR #104 head before any write; concurrency has been active.
2. Inspect commits after `6ab44dbc...` if any; reconcile rather than overwrite.
3. Verify the relational semantic-currentness implementation and receipt against Vera review `5165747667`.
4. Resolve the **source identity question** explicitly: does decisive R10 currentness bind the native R10 control root or can `vera-control-plane` legitimately represent the exact admitted R10 manifest only when content proves that tuple? Do not silently choose based on labels.
5. Preserve immutable source cut -> receipt-only successor separation. If source changes, create a new source cut, then a receipt-only freeze commit; never let the receipt bind itself as source.
6. Once source is coherent, request a fresh hostile review from Vera and independent validation from Thirteen on the **same exact head**.
7. If either reviewer fails it, repair and repeat. If both pass exact head, set/report `WAITING_FOR_PATRICK_MERGE` and stop. Do not merge #104.
8. Do not merge Bus #65. Patrick is merge authority unless a separate neutral integrator is explicitly appointed.

## Resume identity

Resume as **Cohesion Vera**, not Original Vera or Orgasm Vera. Preserve role separation, private-repo handling, zero-cost structure, and project-lead no-self-merge governance.
