# Default Vera Research Surface Audit V1

Status: `BOUNDED_RESEARCH_SURFACE_AUDIT`

Purpose: record which exposed surfaces materially informed the Default Vera V1 training design and where negative/limited results occurred. This is provenance, not proof of exhaustive discovery.

## GitHub

Inspected relevant Vera architecture, behavior, memory, correction, validation, and current R9A0 design evidence. The V1 trainer branch now contains:

- `RESEARCH_FINDINGS_V1.md`
- `RESEARCH_FINDINGS_V2_FINAL.md`
- `CANDIDATE_BOOTSTRAP_PROMPT_V1.md`
- `TRAINING_RUNBOOK_V1.md`
- `EXTERNAL_QUALIFICATION_SPEC_V1.md`
- this surface audit.

Current R9A0 Anticipatory Pragmatics V2 was read at immutable Masamune head:

`d65bcb2f6eb02ae2f9ce75646dc578967fe55c14`

Material AP invariants retained for Default Vera training include evidence-bound presentation hints, fixed protected semantic conclusions, no free-text hidden-user hypothesis channel, no new persistent profile store, and zero additional inference calls at the design level.

## Supabase coordination

Targeted reads covered behavior/interaction/pragmatics work and the current AP review chain.

Material observed coordination included:

- Masa AP V2 design candidate;
- Mune exact-head approval of the AP data contract;
- Bob deterministic evaluation-harness packet;
- Masa non-persistence conformance handoff;
- Hephaestus native-architecture assignment status.

A later targeted read for AP/Default-Vera events after sequence 3320 returned no additional matching rows at that read time.

Coordination rows are treated as coordination evidence, not automatic proof of repository mutation, target consumption, deployment, or production effect.

## File Library / Project historical artifacts

Research used relevant historical and current Vera artifacts including:

- Project Instructions / compact runtime generations;
- Migration Batches 1–3 and provenance-hardened records;
- generic VERA architecture design and audit;
- cognitive-repair behavior/adversarial training artifacts;
- ordinary-mode / unique-insights passdowns;
- current R8A3 native Project instructions and validation lineage.

Private intimate/relational material was not copied into the portable training baseline. Only narrow abstracted behavior lessons with defensible scope were retained.

## Slack expansion

Patrick explicitly authorized searching private Slack/DMs and creating new DMs for this research/coordination task.

Private-surface searches were attempted for behavior-relevant terms including:

- `"do not manufacture endings"`
- `"generic assistant"`
- `"speech act"`
- `Vera`
- `Patrick`

The completed private searches returned no relevant historical message results. Some Slack requests encountered transient `429 rate limited` responses; later same-route reads/searches succeeded. Therefore the bounded conclusion is:

`NO_RELEVANT_PRIVATE_SLACK_HISTORY_SURFACED_IN_TARGETED_SEARCH`

This is not a claim that every private Slack object was exhaustively enumerated.

Public Slack channel discovery found:

- `#build-team-vera` (`C0BNYGECELC`)
- `#build-team-two` (`C0BNTG9RHHA`)

The observed Build Team Two channel itself states that Slack coordination evidence is not repository/database persistence evidence, consistent with the current Vera authority/effect model.

A non-assignment cross-check request was posted to `#build-team-vera` asking active lanes to surface any already-existing, non-private findings that materially contradict or sharpen the Default Vera V1 behavior baseline. No team reply is claimed by this record.

## Private evaluator surface

Two exact external holdout scripts and evaluator anchors were written to a private Slack DM with Patrick rather than committed to the candidate/trainer repository path.

This separation is intentional:

- repository files define evaluation families and scoring;
- exact holdout prompts/anchors remain evaluator-only;
- candidate-accessible training state does not contain answer keys.

The private DM write is only evidence that the messages were sent to Slack. It is not proof that a future evaluator consumed them.

## Google Drive

Earlier research artifacts and continuity materials were available through existing Vera/Hephaestus workflows. No new private Default Vera holdout document was created in Drive during this final pass because private Slack DM provided a smaller evaluator-only surface and avoided duplicating hidden prompts across another searchable source.

## Web / current OpenAI product documentation

The preceding Default Vera research pass used recent official OpenAI Project/Chat behavior evidence already gathered during Hephaestus qualification and Vera research. During the final V2 synthesis, fresh live web attempts encountered repeated transient service errors, so no stronger new product claim was added merely to make the source list look busier.

Exact branch/move/isolation behavior remains a qualification-time empirical dependency and must be reverified before the holdout workflow relies on it.

## Explicitly excluded active surface

Basic Memory Cloud remains a disconnected paid legacy service under current Vera routing. It was not searched, reconnected, renewed, or used as a fallback.

## Audit conclusion

The research reach is sufficient for V1 training design because independent surfaces converge on the same load-bearing behavior boundaries and further archaeology is producing diminishing returns rather than new architecture.

```text
RESEARCH_SURFACE_COVERAGE = SUFFICIENT_FOR_V1_DESIGN
PRIVATE_SLACK_SEARCH = BOUNDED_NO_RELEVANT_HISTORY_SURFACED
EXACT_HOLDOUTS = PREPARED_ON_PRIVATE_EVALUATOR_SURFACE
CANDIDATE_ANSWER_KEY_EXPOSURE = NOT_INTENTIONALLY_CREATED
HISTORICAL_ARCHAEOLOGY = CLOSED_PENDING_MATERIAL_CONTRADICTION
```
